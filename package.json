{
  "name": "yoto-moon-phase",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "wrangler dev",
    "deploy": "wrangler deploy",
    "format": "prettier . --write",
    "check": "prettier . --check"
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "wrangler": "^4.33.0"
  }
}
